'use client';
import { useEffect,useState } from 'react';
export default function AuditStatistics(){const [data,setData]=useState<any>(),[error,setError]=useState('');useEffect(()=>{fetch('/api/v1/audit/statistics').then(r=>r.ok?r.json():Promise.reject(Error('Unable to load audit statistics'))).then(setData).catch(e=>setError(e.message))},[]);if(error)return <p role="alert">{error}</p>;if(!data)return <p>Loading…</p>;const actors=data.actions_by_actor??data.by_actor??{},days=data.actions_by_day??data.by_day??{};if(!Object.keys(actors).length&&!Object.keys(days).length)return <p role="status">No audit events recorded.</p>;return <main><h1>Audit statistics</h1><section><h2>Actions by actor</h2>{Object.entries(actors).map(([k,v])=><p key={k}>{k}: {String(v)}</p>)}</section><section><h2>Actions by day</h2>{Object.entries(days).map(([k,v])=><p key={k}>{k}: {String(v)}</p>)}</section></main>}
